node_modules/
tools/node_modules/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.netlify/
.cache/
# Rust build output (games/black-cat/rust). The COMPILED module is committed
# beside the game as games/black-cat/sim.wasm — the deploy has no build step —
# but the 200MB target/ directory must not be.
target/
**/target/

# verify.mjs output. Regenerated on every run by every worker in parallel, so
# committing them only produces binary merge conflicts. The directory is kept.
screenshots/*.png
